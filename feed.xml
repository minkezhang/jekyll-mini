---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>

{% assign l = site.baseurl | prepend: site.url %}

<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <link>{{ l }}/</link>
    <atom:link href="{{ "/feed.xml" | prepend: l }}" rel="self" type="application/rss+xml"/>
    <pubDate>{{ site.time | date_to_rfc822 }}</pubDate>
    <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
    <generator>Jekyll v{{ jekyll.version }}</generator>

    {% for post in site.posts limit: 10 %}

      {% assign u = post.url | prepend: l %}
      <item>

        <title>{{ post.title | xml_escape }}</title>
        <description>{{ post.content | replace_link: u | xml_escape }}</description>
        <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
        <link>{{ u }}</link>
        <guid isPermaLink="true">{{ u }}</guid>

        {% for tag in post.tags %}
          <category>{{ tag | xml_escape }}</category>
        {% endfor %}

        {% for cat in post.categories %}
          <category>{{ cat | xml_escape }}</category>
        {% endfor %}

      </item>

    {% endfor %}

  </channel>
</rss>
